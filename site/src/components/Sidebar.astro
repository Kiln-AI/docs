---
// starlight-theme-black replaces Starlight's sidebar with a flat one that has
// no collapsible sections. Our SUMMARY.md relies on expanding groups, so we
// take the override back and render Starlight's default sidebar instead.
// The theme detects a user-defined `Sidebar` and skips its own (it logs a
// warning saying so, which is expected).
import Default from '@astrojs/starlight/components/Sidebar.astro';
---

<Default><slot /></Default>
